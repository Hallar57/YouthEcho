{
  "name": "youthecho-functions",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.9.0",
    "firebase-functions": "^7.2.5",
    "groq-sdk": "^1.2.0",
    "resend": "^6.12.3"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
